# klassi Automated Testing Tool
# Created by Larry Goddard
# Javascript Node CircleCI 2.1 configuration file
# Check https://circleci.com/docs/2.0/language-javascript/ for more details

version: 2.1

references:
  filter_ignore_wip_branches:
    filters:
      branches:
        ignore:
          - /[a-z]{2}_wip.*/ # wip branches with initials prefix, e.g. mw_wip
          - /wip\/.*/ # wip branches with wip/ prefix, e.g. wip/foo
          - /poc\/.*/ # poc branches with poc/ prefix, e.g. poc/bar
          - /automation\/.*/ # automation branches with automation/ prefix. e.g. automation/foo
          - /feature\/.*/ # automation branches with feature/ prefix. e.g. feature/foo

    filter_only_integration_branches: &filter_only_integration_branches
      filters:
        branches:
          only:
            - develop # git flow production branch
            - /testfix\/.*/ # automation branches with testfix/ prefix. e.g. testfix/foo

    filter_only_testable_branches: &filter_only_testable_branches
      filters:
        branches:
          only:
            - master # git flow master branch
            - main # git flow main branch
            - develop # git flow develop branch
            - /hotfix\/.*/ # git flow hotfix branches
            - /release\/.*/ # git flow release branches
            - /testfix\/.*/ # testfix branches with testfix/ prefix. e.g. testfix/foo

    filter_only_releasable_branches: &filter_only_releasable_branches
      filters:
        branches:
          only:
            - master # git flow production branch
            - main # git flow production branch
            - /hotfix\/.*/ # git flow hotfix branches
            - /release\/.*/ # git flow release branches

jobs:
  #########################
  ### build and install ###
  #########################
  build_install: &build_install
    docker:
      - image: cimg/node:20.18.3-browsers
    resource_class: small
    working_directory: ~/klassijs

    steps:
      # Install pnpm globally
      - run:
          name: Install pnpm
          command: |
            export SHELL=/bin/bash
            curl -fsSL https://get.pnpm.io/install.sh | sh -

      # Add PNPM to the path
      - run:
          name: Set up PNPM environment
          command: export PATH="$HOME/.local/share/pnpm:$PATH"

      # Verify pnpm installation
      - run:
          name: Check pnpm version
          command: pnpm --version

      - run:
          name: Avoid hosts unknown for github
          command: mkdir ~/.ssh/ && echo -e "Host github.com\n\tStrictHostKeyChecking no\n" > ~/.ssh/config

      - run:
          name: Remove the old klassi-js Repo
          command: rm -rf ~/klassijs/klassi-js

      - run:
          name: Checkout the klassi-js framework repo at v6.1.5
          command: |
            git clone git@github.com:klassijs/klassi-js.git
            cd klassi-js
            git fetch --all --tags
            git checkout v6.1.5

      - run:
          name: Check out the working branch
          path: ~/klassijs/klassi-js
          command: git checkout ${CIRCLE_BRANCH}

      # Generate a robust dependency cache key using SHA-256
      - run:
          name: Generate dependency cache key
          command: |
            if [ -f ~/klassijs/klassi-js/pnpm-lock.yaml ]; then
              shasum -a 256 ~/klassijs/klassi-js/pnpm-lock.yaml > CACHE_KEY
            else
              echo "pnpm-lock.yaml not found, using package.json instead"
              shasum -a 256 ~/klassijs/klassi-js/package.json > CACHE_KEY
            fi

      # Restore cache if available
      - restore_cache:
          name: Restore pnpm cache
          keys:
            - klassi-pnpm-cache-v1-{{ checksum "CACHE_KEY" }}

      - run:
          name: Install the klassi-js framework dependencies
          path: ~/klassijs/klassi-js
          command: |
            git config --global url."git@github.com:".insteadOf "https://github.com/"
            pnpm install --no-frozen-lockfile

      - run:
          name: Run pre-commit checks
          command: |
            cd ~/klassijs/klassi-js
            chmod +x ./runtime/scripts/precommit-checks.sh
            ./runtime/scripts/precommit-checks.sh

      ##########################################################################
      # Save pnpm cache, don't include node modules because we end up with an  #
      # archive so large that unarchiving takes longer than the pnpm install   #
      ##########################################################################
      # Save cache
      - save_cache:
          key: klassi-pnpm-cache-v1-{{ checksum "CACHE_KEY" }}
          paths:
            - ~/klassijs/klassi-js/.cache/pnpm
            - ~/klassijs/klassi-js/.pnpm-store  # PNPM's global store for caching dependencies

      - persist_to_workspace:
          root: ./
          paths:
            - ./klassi-js

  build_and_install:
    <<: *build_install

  ###########################################
  #    s3 Report base configuration         #
  ###########################################
  s3_report_base: &s3_report_base
    docker:
      - image: cimg/node:20.18.3-browsers
    resource_class: small
    steps:
      - attach_workspace:
          at: ./

      - run:
          name: Avoid hosts unknown for github
          command: mkdir ~/.ssh/ && echo -e "Host github.com\n\tStrictHostKeyChecking no\n" > ~/.ssh/config

      - run:
          name: Create projects directory
          command: mkdir ./projects

      #####################################################
      ##            Checkout Project Repos               ##
      #####################################################
      - run:
          name: Checkout the Project Repos
          path: ./projects
          command: |
            exec < "../klassi-js/runtime/scripts/projectList.csv"
            read -r header
            while IFS=", ", read -r name branch folder; do
              if test "$branch"; then
                echo "#################################################### "
                echo "# Cloning the $name Project                          "
                echo "#################################################### "
                git clone -b $branch git@github.com:klassijs/$name.git || true
              else
                git clone -b $branch git@github.com:klassijs/$name.git || true
              fi
            done

      #########################
      ## s3 report Execution ##
      #########################
      - run:
          name: Executing the s3 Report
          path: ./projects
          command: |
            cd ../klass-js
            chmod +x ./runtime/scripts/run-s3-report-all-projects.sh
            ./runtime/scripts/run-s3-report-all-projects.sh ../projects

  s3Report_email_run:
    <<: *s3_report_base

  ###########################################
  #   Unit test base configuration   #
  ###########################################
  unit_test_base: &unit_test_base
    docker:
      - image: cimg/node:20.18.3-browsers
    resource_class: small
    steps:
      - attach_workspace:
          at: ./

      - run:
          name: Setting up tmp Dir for split files
          command: mkdir ./tmp

      - run:
          name: Run unit tests
          path: ./klassi-js
          command: |
            pnpm test:checkin

      - store_test_results:
          path: ./klassi-js/reports

  unit_test:
    <<: *unit_test_base

  ###########################################
  #   Integration test base configuration   #
  ###########################################
  integration_test_base: &integration_test_base
    docker:
      - image: cimg/node:20.18.3-browsers
    resource_class: small
    steps:
      - attach_workspace:
          at: ./

      - run:
          name: Setting up tmp Dir for split files
          command: mkdir ./tmp

      - run:
          name: Run integration tests
          path: ./klassi-js
          command: |
            pnpm test:merge

      - store_test_results:
          path: ./klassi-js/reports

  integration_test:
    <<: *integration_test_base

workflows:
  version: 2
  build_and_test:
    when: << pipeline.parameters.run_release >>
    jobs:
      - build_and_install:
          context: klassi-framework
          <<: *filter_only_testable_branches

      - unit_test:
          context: klassi-framework
          requires:
            - build_and_install
          <<: *filter_only_testable_branches

      - integration_test:
          context: klassi-framework
          requires:
            - unit_test
          <<: *filter_only_integration_branches

#  s3Report_run:
#    triggers:
#      - schedule:
#          cron: "00 04 * * *"
#          filters:
#            branches:
#              only:
#                - main
#
#    jobs:
#      - build_and_install:
#          context: klassi-framework
#
#      - s3Report_email_run:
#          context: klassi-framework
#          requires:
#            - build_and_install

  s3Report_manual:
    when: << pipeline.parameters.run_s3_report >>
    jobs:
      - build_and_install:
          context: klassi--framework

      - s3Report_email_run:
          context: klassi-framework
          requires:
            - build_and_install